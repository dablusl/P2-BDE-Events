@model P2_BDE_Events.ViewModels.TousLesEvenementsViewModel

@{
    Layout = "~/Views/Shared/_LayoutOrganisateur.cshtml";
}

<link rel="stylesheet" href="~/css/Participant/AgendaEvenement.css">

@section Header {
    <ul>
        <li class="search-form">
            <form method="get" action="@Url.Action("TousLesEvenement", "TousLesEvenements", new { area = "Organisateur" })">
                <input type="hidden" name="universite" value="@Model.Organisateur.Participant"/>
                <button type="submit">Les évènements de mon université</button>
            </form>
        </li>
        <li>
            <form method="get">
                <input type="text" name="nomEvent" placeholder="Rechercher par nom de l'évènement'" />
                <button type="submit">Rechercher</button>
            </form>
        </li>
    </ul>
}


@foreach (var evenement in Model.Evenements)
{
    <a href="@Url.Action("SolliciterEvenement", "TousLesEvenements", new { area = "Organisateur", EvenementID = evenement.Id })" class="voirPlus">
        <div class="evenement">
            <div class="photo">
                <img src="@evenement.CoverPhotoPath" alt="">
            </div>
            <div class="info">
                <div class="prix">
                    <p>@evenement.PrixBillet €</p>
                </div>
                <div class="date">
                    <p>@evenement.DateEvenement.ToShortDateString()</p>
                </div>
                <div class="participant">
                    <p>@evenement.NbParticipants/@evenement.MaxParticipants</p>
                </div>
            </div>
        </div>
    </a>
}

